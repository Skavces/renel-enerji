services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: renel
      POSTGRES_PASSWORD: renel_pass
      POSTGRES_DB: renel_enerji
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U renel -d renel_enerji"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    restart: unless-stopped
    expose:
      - "3001"
    environment:
      PORT: "3001"
      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: renel
      DB_PASS: renel_pass
      DB_NAME: renel_enerji
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: 8h
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD_HASH: ${ADMIN_PASSWORD_HASH}
      FRONTEND_URL: "http://141.98.51.126"
      UMAMI_URL: http://umami:3000
      UMAMI_WEBSITE_ID: ${UMAMI_WEBSITE_ID}
      UMAMI_USER: ${UMAMI_USER:-admin}
      UMAMI_PASS: ${UMAMI_PASS:-umami}
    volumes:
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://141.98.51.126
        VITE_UMAMI_URL: http://141.98.51.126:3002
        VITE_UMAMI_WEBSITE_ID: ${UMAMI_WEBSITE_ID}
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend

  umami-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: umami_pass
      POSTGRES_DB: umami
    volumes:
      - umami_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U umami -d umami"]
      interval: 5s
      timeout: 5s
      retries: 10

  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      DATABASE_URL: postgresql://umami:umami_pass@umami-db:5432/umami
      APP_SECRET: ${UMAMI_APP_SECRET:-renel-umami-secret-change-in-production}
    depends_on:
      umami-db:
        condition: service_healthy

volumes:
  pgdata:
  uploads:
  umami_pgdata:
